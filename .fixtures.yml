fixtures:
  symlinks:
    fluent_bit: "#{source_dir}"
  repositories:
    apt: "git://github.com/puppetlabs/puppetlabs-apt.git"
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
    yum:
      repo: "puppetlabs/yum"

